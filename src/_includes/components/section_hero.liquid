<div class="relative">
  <img src="{{ section.image }}" class="absolute inset-0 object-cover w-full h-full" />
  <div class="relative bg-gray-900 bg-opacity-90">
    <div class="py-8 px-4 mx-auto sm:max-w-xl md:max-w-full lg:py-12 lg:max-w-screen-xl">
      <div class="w-full">
        <h2 class="text-center mb-8 font-sans text-3xl font-bold tracking-tight text-white sm:text-4xl sm:leading-none">
          {{ section.title }}
        </h2>
        <p class="text-center text-lg text-gray-200">
          {{ section.subtitle }}
        </p>
      </div>
    </div>
  </div>
</div>